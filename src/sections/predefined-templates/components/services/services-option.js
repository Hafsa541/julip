import { Typography } from '@mui/material';

// ----------------------------------------------------------------------

function ServicesOption() {
  return <Typography variant="h6">Services Section</Typography>;
}

export default ServicesOption;
