// sections
import { JwtOnboardingView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: Onboarding',
};

export default function OnboardingPage() {
  return <JwtOnboardingView />;
}
